1. 查看当前状态。git status
2. 提交本地更改。git add .    git commit -m "feat（登录页面)：新增登录页面"
3. 拉取远程分支的最新代码。git fetch获取最新代码，git merge进行合并。
4. 解决冲突，如果在合并过程中发生冲突，需要手动打开文件解决并修改，然后标记为已解决。
5. 推送到远程仓库。git push origin 本地分支